# Frontend (React + TypeScript)

## Local run

```bash
cd frontend
npm install
npm run dev
```

Set API base if needed:

```bash
VITE_API_BASE_URL=http://localhost:8000 npm run dev
```

The page includes:
- system connectivity status
- auth testing panel (register/login/me)
